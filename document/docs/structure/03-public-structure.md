```txt
public || arquivos estáticos servidos diretamente pelo servidor
│
├─ .htaccess   || configuração Apache para SPA (redireciona rotas não encontradas para `index.html`).
├─ favicon.svg || ícone principal do site exibido na aba do navegador.
│
└─ images || imagens públicas usadas no front
    │
    └─ blog || imagens do módulo de blog
        │
        └─ covers || capas de artigos do blog
            │
            └─ placeholder-1.svg || capa placeholder (1200x675) com gradiente e texto "TODDYNHO BLOG".
```
